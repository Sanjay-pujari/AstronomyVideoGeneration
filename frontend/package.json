{
  "name": "astropulse-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "test": "npm run build && node --test test/*.test.mjs",
    "dev": "vite --port 3000",
    "preview": "npm run build && node scripts/spa-server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.11"
  }
}
